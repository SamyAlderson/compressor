[package]
name = "compressor"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = "0.8.3"
bincode = "1.3.2"

[features]
compression = []
run-length encoding = []

[profile.release]
opt-level = "s"
lto = true

[features]
compression = []
encoding = []
```